#include <stdlib.h>
#include "main.h"

/**
 * main - it prints the program name, followed by a new line..
 *
 * @argc: checks input of the function.
 * @argv: checks input of the function.
 *
 * Return: The function should return (0) success.
*/

int main(int argc, char const *argv)
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
