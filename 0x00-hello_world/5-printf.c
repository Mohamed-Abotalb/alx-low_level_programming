#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: The main function should print a message using printf function
 *
 * Return: the main function should return 0 (success)
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
